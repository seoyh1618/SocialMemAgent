Genera mappe di codice navigabili per basi di codice sconosciute. Da utilizzare quando si esplora una nuova base di codice, è necessario comprendere la struttura del progetto o prima di immergersi nelle modifiche del codice. Estrae esportazioni/importazioni tramite AST (tree-sitter) per creare file _MAP.md per directory. Si attiva su "mappa questa base di codice", "comprendi la struttura di questo progetto", "genera la mappa di codice" o quando inizi a lavorare su un repository sconosciuto.
