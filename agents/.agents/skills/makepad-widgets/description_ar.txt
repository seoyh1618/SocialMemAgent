هام: يستخدم لعناصر واجهة المستخدم Makepad ومكونات واجهة المستخدم. المشغلات على:
أداة makepad، عرض makepad، زر makepad، تسمية makepad، صورة makepad،
makepad TextInput، RoundedView، SolidView، ScrollView، "مكون makepad"،
makepad Markdown، makepad Html، TextFlow، نص منسق، 富文本، markdown渲染،
makepad، makepad، makepad، makepad، makepad، makepad، makepad
