Flusso di lavoro end-to-end per il debug degli errori del fuzzer SSA da CI. Estrae un caso di riproduzione dai log di GitHub Actions, quindi divide in due i passaggi SSA per identificare il bug. Da utilizzare quando un test `pass_vs_prev` o un test fuzzer simile fallisce nel CI.
