Escriba un protocolo de revisión sistemática en `output/PROTOCOL.md` (bases de datos, consultas, inclusión/exclusión, ventana de tiempo, campos de extracción).
**Desencadenante**: protocolo, PRISMA, revisión sistemática, inclusión/exclusión, 检索式, 纳入排除.
**Usar cuando**: proceso de revisión sistemática 的起点（C1），需要先锁定 protocolo 再开始 screening/extracción。
**Omitir si**: 不是做 revisión sistemática（或 protocolo 已经锁定且不允许修改）。
**Red**: ninguna.
**Guardrail**: protocolo 必须包含可执行的检索与筛选规则；需要 HUMAN 签字后才能进入 screening。
