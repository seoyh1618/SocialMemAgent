Genera configurazioni Docker pronte per la produzione per API .NET con build in più fasi, ottimizzazione Alpine, memorizzazione nella cache dei livelli e script di build. Da utilizzare durante la containerizzazione di applicazioni .NET, la creazione di Dockerfile o l'ottimizzazione delle configurazioni Docker esistenti.
