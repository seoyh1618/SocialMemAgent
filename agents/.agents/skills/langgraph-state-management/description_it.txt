Progetta schemi di stato, implementa riduttori, configura la persistenza ed esegui il debug dei problemi di stato per le applicazioni LangGraph. Utilizzare quando gli utenti desiderano (1) progettare o definire schemi di stato per i grafici LangGraph, (2) implementare funzioni di riduzione per l'accumulo di stati, (3) configurare la persistenza con checkpointer (InMemorySaver/MemorySaver, SqliteSaver, PostgresSaver), (4) eseguire il debug di problemi di aggiornamento dello stato o comportamenti di stato imprevisti, (5) migrare schemi di stato tra versioni, (6) convalidare la struttura dello schema di stato, (7) scegliere tra modelli TypedDict e MessagesState, (8) implementare riduttori personalizzati per elenchi, dict o set, (9) utilizzare il tipo Sovrascrivi per ignorare i riduttori, (10) impostare la persistenza basata su thread per conversazioni a più turni o (11) ispezionare i checkpoint per il debug.
