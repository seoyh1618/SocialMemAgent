標準化跨技能 JSONL 接口（ids + 標題 + 引文關鍵格式），以便下游技能不依賴於盡力而為的連接。
**觸發器**：模式規範化、jsonl契約、接口漂移、連接漂移、字段不一致、模式規範化。
**使用時間**：您已生成 C2-C4 JSONL 工件（大綱/簡報/綁定/包/錨點），並且在自循環/寫入之前需要確定性、穩定的字段。
**如果**則跳過：您沒有使用調查管道，或者工作區已經為當前工件提供了新的 PASS `output/SCHEMA_NORMALIZATION_REPORT.md`。
**網絡**：無。
**護欄**：沒有散文；僅確定性變換；不要發明證據/主張；僅填充 `outline/outline.yml` 中缺少的 ids/title。
