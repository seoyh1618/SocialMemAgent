标准化跨技能 JSONL 接口（ids + 标题 + 引文关键格式），以便下游技能不依赖于尽力而为的连接。
**触发器**：模式规范化、jsonl契约、接口漂移、连接漂移、字段不一致、模式规范化。
**使用时间**：您已生成 C2-C4 JSONL 工件（大纲/简报/绑定/包/锚点），并且在自循环/写入之前需要确定性、稳定的字段。
**如果**则跳过：您没有使用调查管道，或者工作区已经为当前工件提供了新的 PASS `output/SCHEMA_NORMALIZATION_REPORT.md`。
**网络**：无。
**护栏**：没有散文；仅确定性变换；不要发明证据/主张；仅填充 `outline/outline.yml` 中缺少的 ids/title。
