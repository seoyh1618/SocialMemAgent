Ottimizzazione sistematica delle prestazioni e debug della regressione per l'app mobile OneKey (iOS). Da utilizzare quando: (1) Correzione delle regressioni delle prestazioni - quando metriche come tokensStartMs, tokensSpanMs o functionCallCount sono regredite e devono essere riportate ai livelli normali, (2) Miglioramento delle prestazioni di base - quando è necessario ottimizzare il tempo di avvio a freddo o ridurre il sovraccarico delle chiamate di funzione, (3) L'utente richiede l'ottimizzazione/miglioramento/debug delle prestazioni per l'avvio dell'app o il flusso di aggiornamento della schermata iniziale.
