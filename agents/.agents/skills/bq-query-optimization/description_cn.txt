在编写 BigQuery 查询、优化查询性能、分析执行计划或避免常见 SQL 问题时使用。涵盖参数化查询、UDF、脚本编写、窗口函数（QUALIFY、ROW_NUMBER、RANK、LEAD/LAG）、JSON 函数、ARRAY/STRUCT 操作、BigQuery 特定功能（EXCEPT、REPLACE、SAFE_*）、CTE 重新执行问题、NOT IN with NULL、DML 性能、标准与旧版 SQL 以及性能最佳实践。
