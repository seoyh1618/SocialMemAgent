Padronanza di Redis per operazioni di memorizzazione nella cache, strutture dati, pub/sub e CLI. Da utilizzare quando l'utente chiede di "configurare Redis", "cache dei dati", "comandi Redis", "pub/sub", "tipi di dati Redis", "archivio sessioni", "limitazione della velocità con Redis" o qualsiasi attività Redis.
