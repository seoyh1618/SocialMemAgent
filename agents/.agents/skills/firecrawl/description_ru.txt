Веб-скрапинг и поисковый интерфейс командной строки возвращают чистый Markdown с любого URL-адреса (обрабатывает JS-рендеринг).
страницы, SPA). Используйте, когда пользователь запрашивает: (1) «поиск X в Интернете», (2) «очистить/извлечь».
URL-контент», (3) «получить контент с веб-сайта», (4) «найти последние статьи о X»,
(5) исследовательские задачи, требующие текущих веб-данных, (6) извлечение структурированных данных со страниц.

Выводит дружественный к LLM Markdown, обрабатывает аутентификацию через вход в систему с помощью FireCrawl, поддерживает
параллельный парсинг для массовых операций. Автоматически записывает в каталог .firecrawl/.

Триггеры: парсинг веб-страниц, поиск в сети, получение URL-адреса, извлечение контента, Firecrawl, парсинг.
веб-сайт, получение содержимого страницы, веб-исследования, карта сайта, сканирование сайта.
