App iOS Scaffold con Tuist e architettura a più livelli (Dominio, Infrastruttura, App).
Utilizzare quando: (1) Creare un nuovo progetto di app iOS, (2) Configurare la struttura del progetto Tuist,
(3) L'utente chiede di "creare un'app iOS", "impalcatura di un'app" o "impostare un nuovo progetto Swift",
(4) L'utente desidera un'architettura a più livelli/pulita per iOS, (5) L'utente menziona la configurazione Tuist.
