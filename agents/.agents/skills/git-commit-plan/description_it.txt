Il piano si impegna dall'albero di lavoro corrente. Da utilizzare quando viene chiesto come suddividere le modifiche in commit, cosa mettere in stage insieme, come gestire i file non tracciati o per proporre messaggi di commit e un piano di esecuzione (stage/commit) basato su `git status`/diffs.
