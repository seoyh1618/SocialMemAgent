Esegui controlli fumogeni post-distribuzione con target Makefile (`remote-status`, `remote-logs`) oltre a controlli facoltativi dell'URL di integrità. Da utilizzare dopo la distribuzione per verificare lo stato di runtime prima dell'accettazione finale.
