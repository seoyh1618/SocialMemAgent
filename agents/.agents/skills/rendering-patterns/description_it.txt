Spiega le strategie di rendering Web tra cui rendering lato client (CSR), rendering lato server (SSR), generazione di siti statici (SSG), rigenerazione statica incrementale (ISR) e streaming. Da utilizzare per decidere la strategia di rendering, ottimizzare le prestazioni o capire quando il contenuto viene generato e inviato agli utenti.
