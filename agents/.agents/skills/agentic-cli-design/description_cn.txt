构建 LLM/AI 代理可以安全可靠运行的 CLI 工具的设计原则。
提供 7 个核心原则（机器可读、非交互、幂等、默认安全、
可观察、上下文有效、可自省），带有记分卡、模板和反模式。
在以下情况下使用：(1) 设计新的 CLI 工具供代理使用，(2) 改进现有的 CLI 供代理使用
兼容性，(3) 检查 CLI 设计是否适合代理，或者 (4) 需要以下方面的指导
JSON 输出格式、退出代码、身份验证流程或自描述 CLI 模式。
