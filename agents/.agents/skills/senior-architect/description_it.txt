Questa competenza dovrebbe essere utilizzata quando l'utente chiede di "progettare l'architettura del sistema", "valutare microservizi rispetto a monoliti", "creare diagrammi di architettura", "analizzare le dipendenze", "scegliere un database", "pianificare la scalabilità", "prendere decisioni tecniche" o "esaminare la progettazione del sistema". Utilizzare per record decisionali di architettura (ADR), valutazione dello stack tecnologico, revisioni della progettazione di sistema, analisi delle dipendenze e generazione di diagrammi di architettura in formato Sirena, PlantUML o ASCII.
