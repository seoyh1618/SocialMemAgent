إرشادات تطوير Firebase الشاملة للتطبيقات التي يستضيفها Google Cloud Platform. يغطي عمليات قاعدة بيانات Firestore (CRUD، والاستعلامات، والمعاملات، ونمذجة البيانات)، والوظائف السحابية (الجيل الأول والثاني، وTypeScript وPython، وجميع أنواع المشغلات)، وعمليات Firebase CLI، وإعداد المحاكي واستمرارية البيانات، وقواعد الأمان (Firestore والتخزين)، وتكامل المصادقة، وتهيئة الاستضافة، وتكامل خدمة GCP. يُستخدم عند العمل مع مشاريع Firebase، ونشر وظائف السحابة، والاستعلام عن Firestore، وإعداد المشغلات (Firestore، وAuth، وStorage، وHTTP، وCallable، وScheduled، وPub/Sub)، وإدارة قواعد الأمان، وتهيئة عمليات إعادة الكتابة/رؤوس الاستضافة، وإدارة الأسرار، أو التكامل مع خدمات GCP مثل BigQuery وCloud Tasks. تتضمن المشغلات Firebase، وfirestore، والوظائف السحابية، ووظائف Firebase، واستضافة Firebase، ومصادقة Firebase، وتخزين Firebase، ومحاكي Firebase، ونشر Firebase، وFirebase init، وقواعد Firebase، والوظيفة القابلة للاستدعاء، والوظيفة المجدولة، وonDocumentCreated، وonRequest، وonCall، وonSchedule.
