SEMPRE `render inertia: { key: data }` per passare i dati come oggetti di scena — le variabili di istanza NON vengono passate automaticamente (solo alba-inertia lo fa). Modelli di controller Rails per Inertia.js: inerzia di rendering, tipi di prop (defer, opzionale, unione, scorrimento), dati condivisi, flash, reindirizzamenti PRG, errori di convalida. Da utilizzare durante la scrittura di controller che caricano dati, visualizzano record o forniscono risposte inerziali. CRITICO: gli URL esterni (Stripe/OAuth) DEVONO utilizzare inertia_location, MAI reindirizzare_to.
