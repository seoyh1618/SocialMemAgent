Ably Realtime 的专业指南，用于构建具有发布-订阅消息传递、通道、状态跟踪、Spaces（协作 UI）、LiveObjects（共享状态）、Chat SDK（完整消息传递）和 LiveSync（数据库同步）的实时 React/TypeScript 应用程序。在使用 Ably、实时消息传递、WebSockets、pub-sub、通道、状态、协作功能、实时光标、头像堆栈、组件锁定、共享状态、无冲突更新、聊天室、打字指示器、消息反应、数据库同步、发件箱模式、useChannel、usePresence、useSpace、useMembers、useCursors、useMessages、useTyping、LiveCounter、LiveMap 或将 Ably 与 Neon/PostgreSQL 集成以实现持久性时使用实时数据。
