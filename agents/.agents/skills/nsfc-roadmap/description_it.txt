Utilizzare solo quando l'utente richiede esplicitamente di "generare una roadmap tecnologica/un disegno di roadmap/una roadmap/un diagramma di flusso" o ha bisogno di convertire il contenuto della ricerca della proposta in una roadmap tecnologica "stampabile e leggibile in formato A4". Genera file sorgente modificabili (`.drawio`) e rendering incorporabili nei documenti (`.svg`/`.png`/`.pdf`). ⚠️ Non applicabile: l'utente desidera semplicemente modificare il formato/dimensione di un'immagine esistente (usare abilità di elaborazione delle immagini) o semplicemente perfezionare la descrizione testuale della roadmap tecnologica (riscrivere direttamente il testo principale).
