Genera automaticamente report di lavoro giornalieri eseguendo la scansione dei repository Git. Utilizza questa abilità quando l'utente chiede di: (1) generare un report giornaliero da git commit, (2) riepilogare il lavoro svolto in una data specifica in base alle modifiche del codice, (3) controllare i commit e creare un riepilogo del lavoro per una data. La competenza analizza tutti i repository git in una directory root, filtra per autore, recupera i record di commit con le modifiche ai file e genera una descrizione riepilogativa del contenuto del lavoro utilizzando l'analisi LLM.
