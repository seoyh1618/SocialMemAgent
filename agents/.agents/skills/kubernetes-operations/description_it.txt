Assistere con le interazioni Kubernetes incluso il debug (log kubectl, descrizione, exec, port forwarding), gestione delle risorse (distribuzioni, servizi, mappe di configurazione, segreti) e operazioni del cluster (ridimensionamento, implementazioni, gestione dei nodi). Da utilizzare quando si lavora con kubectl, pod, distribuzioni, servizi o per la risoluzione dei problemi di Kubernetes.
