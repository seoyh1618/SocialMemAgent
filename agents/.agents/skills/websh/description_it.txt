Un guscio per il web. Naviga negli URL come directory, interroga pagine con comandi simili a Unix.
Attivalo con il comando `websh`, con la navigazione web in stile shell o quando tratti gli URL come un filesystem.
