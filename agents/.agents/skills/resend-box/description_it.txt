Resend Box è una sandbox di posta elettronica locale che simula l'API Resend e acquisisce le email per l'ispezione. Utilizza questa competenza quando lavori con Resend SDK o SMTP per inviare e-mail durante lo sviluppo. Cattura tutte le email inviate tramite Resend SDK (quando RESEND_BASE_URL punta alla sandbox) o tramite SMTP (porta 1025). Utilizzare per verificare che le e-mail vengano inviate correttamente, ispezionare il contenuto delle e-mail o testare modelli di e-mail senza inviare e-mail reali.
