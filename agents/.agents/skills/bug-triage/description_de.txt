Reproduzieren, isolieren und beheben Sie einen Fehler (oder einen fehlgeschlagenen Build/Test) und fassen Sie dann die Grundursache, die Fehlerbehebung und die Überprüfungsschritte zusammen. Verwenden Sie diese Option, wenn der Benutzer einen Fehler, eine Regression oder einen fehlgeschlagenen Build/Test meldet und eine Lösung wünscht.
