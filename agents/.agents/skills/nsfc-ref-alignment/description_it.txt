Controllare le citazioni del testo principale della proposta NSFC rispetto ai riferimenti per rischi di coerenza e autenticità (sola lettura): verificare l'esistenza della bibkey, i campi BibTeX, il formato DOI, ecc.; generare input strutturati per l'IA host per valutare riga per riga "il testo principale cita davvero questo riferimento"; per impostazione predefinita genera solo un rapporto di audit, non modificare mai la proposta o il file .bib se non esplicitamente richiesto.
