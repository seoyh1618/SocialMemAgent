Break a feature request into dependency-aware implementation tasks and generate a machine-readable task graph. Use when planning or executing feature work that needs task IDs, blockers, critical paths, or parallel work windows.
