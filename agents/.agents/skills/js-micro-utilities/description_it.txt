Utilità JavaScript a dipendenza zero che utilizzano API native e micro-pacchetti just-*. Da utilizzare durante la manipolazione di oggetti, matrici, stringhe, numeri o funzioni. Eseguire prima la scansione delle tabelle per la soluzione nativa (backtick), ricorrere al pacchetto just-* solo quando necessario. Preferisci il nativo alle dipendenze.
