OBLIGATOIRE lorsque l'utilisateur mentionne un module Drupal + erreur/bug/problème - même sans traces de pile. Déclencheur sur : (1) "Le module <module_name> a une erreur/un bug/un problème", (2) "Acquia/Pantheon/Platform.sh" + problème de module, (3) tout nom de module de contribution (balise méta, formulaire Web, mcp, paragraphes, etc.) + description du problème. Recherche Drupal.org AVANT d'écrire un patch. PAS seulement pour les contributions en amont - à utiliser pour TOUS les correctifs locaux de contrib/core.
