Converti i log JSON/JSONL non elaborati di una sessione dell'agente in testo leggibile e genera alcune statistiche di base. Da utilizzare quando è necessario riprodurre una sessione dell'agente o convertire la sessione JSON/JSONL in un formato leggibile dall'uomo.
