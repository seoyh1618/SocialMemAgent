Verwenden Sie diese Option, wenn Sie I2 (Implementierungsausführung) im Spec Pack von sdlc-dev durchführen müssen, wobei „{FEATURE_DIR}/implementation/plan.md“ als einzelnes SSOT für die schrittweise Implementierung verwendet wird, eine minimale Überprüfung ausgeführt wird, Prüfinformationen zurückgeschrieben werden und Berichte an Batch-Prüfpunkten erstellt werden. Stoppen Sie sofort, wenn Sie auf Blocker oder klärende Elemente stoßen.
