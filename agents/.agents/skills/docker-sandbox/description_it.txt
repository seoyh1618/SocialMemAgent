Crea, gestisci ed esegui strumenti agente (claude, codex) all'interno dei sandbox Docker per l'esecuzione di codice isolata. Da utilizzare durante l'esecuzione di loop di agenti, la generazione di sottoprocessi dello strumento o qualsiasi attività che richieda l'isolamento dei processi. Trigger su "sandbox", "esecuzione isolata", "sandbox docker", "esecuzione sicura dell'agente" o quando si lavora sull'infrastruttura del loop dell'agente.
