Genera script di installazione multipiattaforma per qualsiasi software, libreria o modulo. Da utilizzare quando gli utenti chiedono di "creare un programma di installazione", "generare script di installazione", "automatizzare l'installazione", "script di configurazione per X", "installare X su qualsiasi sistema operativo" o necessitano di una distribuzione automatizzata su Windows, Linux e macOS. La competenza genera uno script autonomo `install.sh` (e facoltativamente `install.ps1`) che può essere eseguito tramite un singolo one-liner curl/wget dal contenuto utente non elaborato di GitHub. Segue un approccio in quattro fasi: (1) Esplorazione dell'ambiente: rilevamento del sistema operativo, raccolta di informazioni sul sistema, controllo delle dipendenze; (2) Pianificazione dell'installazione: proporre passaggi con verifica; (3) Generazione di script: produce uno script di installazione autonomo con un comando di una riga; (4) Generazione della documentazione.
