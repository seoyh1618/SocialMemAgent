從“UNITS.csv”中精確執行一個可運行單元（第一個 TODO，其依賴項已完成），然後更新單元狀態和工件。
**觸發器**：單元執行器、運行一個單元、下一個單元、逐步管道、逐條執行、UNITS.csv。
**使用時**：需要嚴格“一次只做一個單元”（可審計、可中斷），並避開檢查點/HUMAN卡邏輯。
**如果**：要端到端自動運行（用 `research-pipeline-runner`）或工作空間不存在，則跳過。
**網絡**：無。
**Guardrail**：只執行一個單元；滿足驗收且輸出存在才可標記“DONE”；遇到HUMAN檢查點必須停止。
