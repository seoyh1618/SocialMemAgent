从“UNITS.csv”中精确执行一个可运行单元（第一个 TODO，其依赖项已完成），然后更新单元状态和工件。
**触发器**：单元执行器、运行一个单元、下一个单元、逐步管道、逐条执行、UNITS.csv。
**使用时**：需要严格“一次只做一个单元”（可审计、可中断），并避开检查点/HUMAN卡逻辑。
**如果**：要端到端自动运行（用 `research-pipeline-runner`）或工作空间不存在，则跳过。
**网络**：无。
**Guardrail**：只执行一个单元；满足验收且输出存在才可标记“DONE”；遇到HUMAN检查点必须停止。
