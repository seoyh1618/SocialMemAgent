Выполните ровно один работоспособный модуль из `UNITS.csv` (первый TODO, зависимости которого DONE), затем обновите статус модуля и артефакты.
**Триггер**: исполнитель модуля, запуск одного модуля, следующий модуль, пошаговый конвейер, 逐条执行, UNITS.csv.
**Используйте, когда**: 需要严格“一次只做一个 unit”（可审计、可中断）,并遵守 контрольно-пропускные пункты/ЧЕЛОВЕЧЕСКИЙ 阻塞逻辑。
**Пропустить, если**: 要端到端自动跑（用 `research-pipeline-runner`）或 workspace 不存在。
**Сеть**: нет.
**Ограждение**: 只执行一个 блок；满足验收且输出存在才可标 `DONE`; контрольно-пропускной пункт ЧЕЛОВЕКА 必须停下。
