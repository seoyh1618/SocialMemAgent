Fornisce modelli di base e best practice per SDK AWS per Java 2.x. Da utilizzare durante la configurazione dei client dei servizi AWS, l'impostazione dell'autenticazione, la gestione delle credenziali, la configurazione dei timeout, dei client HTTP o quando si seguono le best practice dell'SDK AWS.
