A guide on where to place repository interfaces. Explains why, in Clean Architecture, repository interfaces should be placed in the use-case layer rather than the domain layer. Focuses on preventing coupling between domain models and repositories and enforcing design intent through structure. Triggered by repository-placement queries such as “where to put the repository,” “repository interface placement,” “repository in domain layer,” “repository in Clean Architecture,” etc.
