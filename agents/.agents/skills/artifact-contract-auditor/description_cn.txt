根据管道工件契约（DONE 输出 + 管道 target_artifacts）审核工作区。
写入`output/CONTRACT_REPORT.md`。
**触发器**：合同审计、工件合同、缺失工件、target_artifacts、CONTRACT_REPORT。
**使用时间**：您需要一个可审核的“通过/失败”视图，以了解工作区是否完整且独立（运行结束或共享之前）。
**如果**则跳过：您仍然有意进行中期运行并且还不关心完整性（但它作为快照仍然有用）。
**网络**：无。
**护栏**：仅分析；不要编辑内容工件；只写报告。
