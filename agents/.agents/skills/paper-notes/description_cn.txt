将核心集中每篇论文的结构化注释写入“papers/paper_notes.jsonl”（摘要/方法/结果/限制）。
**触发器**：纸质笔记、结构化笔记、阅读笔记、论文笔记、paper_notes.jsonl。
**使用时**：调查的证据阶段（C3），已有`papers/core_set.csv`（以及可选全文），需要为后续声明/引文/写作准备可引用证据。
**如果**则跳过：还没有核心集（先运行 `dedupe-rank`），或者你只做极轻量快照不需要细粒度证据。
**网络**：无。
**Guardrail**：具体可明确对（方法/指标/限制），避免大量重复模板；保持填写字段避免长文。
