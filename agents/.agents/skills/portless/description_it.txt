Configura e utilizza portless per gli URL dei server di sviluppo locali denominati (ad esempio http://myapp.localhost invece di http://localhost:3000). Da utilizzare quando si integra portless in un progetto, si configurano i nomi dei server di sviluppo, si imposta il proxy locale, si lavora con domini .localhost o si risolvono problemi di porta/proxy.
