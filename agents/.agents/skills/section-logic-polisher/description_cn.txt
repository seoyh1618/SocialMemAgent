每个 H3 部分文件的逻辑连贯性通过：在合并之前强制执行明确的第 1 段论文并表面段落岛风险（连接器统计信息是诊断性的，而不是配额）。
**触发器**：逻辑抛光器、章节逻辑、论文陈述、连接器、逻辑逻辑、连接词、运算主线、润色逻辑。
**使用时**： `sections/S*.md` 存在但读起来像段落岛；你想要在“section-merger”之前有一个有针对性的、可调试的自循环。
**如果**则跳过：章节缺失/薄弱（首先修复 `subsection-writer`）或证据包/简报搭建起来（首先修复 C3/C4）。
**网络**：无。
**护栏**：不添加新的引文；不要捏造事实；不要更改引文关键字；不要跨小节移动引用。
