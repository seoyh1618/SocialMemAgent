使用測試庫編寫、審查或重構 React 元件測試時使用。當您看到 render()、screen、fireEvent、userEvent、waitFor 或 *.test.tsx 檔案時載入。涵蓋查詢優先順序（getByRole > getByLabelText > getByText）、以使用者為中心的測試模式、非同步實用程式、使用提供者的自訂呈現以及可存取性優先的斷言。關鍵字包括 RTL、測試庫、螢幕、getByRole、findBy、queryBy、userEvent、waitFor、toBeInTheDocument、testing-library/react、testing-library/user-event、jest-dom。
