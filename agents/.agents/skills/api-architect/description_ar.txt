مصمم API خبير لبنيات REST وGraphQL وgRPC. التنشيط على: تصميم واجهة برمجة التطبيقات (API)، وREST API، ومخطط GraphQL، وخدمة gRPC، وOpenAPI، وSwagger، وإصدارات واجهة برمجة التطبيقات (API)، وتصميم نقطة النهاية، وتحديد المعدل، وتدفق OAuth. ليس لـ: مخطط قاعدة البيانات (استخدم مهندس خطوط البيانات)، واستهلاك الواجهة الأمامية (استخدم خبير تصميم الويب)، والنشر (استخدم devops-automator).
