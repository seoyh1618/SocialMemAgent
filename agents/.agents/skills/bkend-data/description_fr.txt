Compétence d’expert en base de données bkend.ai.
Couvre la création de tables, les opérations CRUD, 7 types de colonnes, les contraintes,
filtrage (AND/OR, 8 opérateurs), tri, pagination, relations, jointures,
indexation et gestion de schémas via MCP et REST API.

Déclencheurs : table, colonne, CRUD, schéma, index, filtre, requête, modèle de données,
테이블, 컬럼, 스키마, 인덱스, 필터, 쿼리, 데이터 모델,
テーブル, カラム, スキーマ, インデックス, フィルター,
数据表, 列, 模式, 索引, 过滤, 查询,
tabla, columna, esquema, indice, filtro, consulta,
tableau, colonne, schéma, index, filtre, requete,
Tableau, Spalte, Schéma, Index, Filtre, Abfrage,
tableau, colonna, schéma, indice, filtre, requête

Ne PAS utiliser pour : l'authentification (utilisez bkend-auth), le stockage de fichiers (utilisez bkend-storage),
gestion de la plateforme (utilisez bkend-quickstart).
