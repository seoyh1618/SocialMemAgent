Convalida la configurazione e la struttura del progetto delle app in lancio sia per le app Web che per quelle React Native. Da utilizzare per verificare se granite.config.ts è configurato correttamente, verificare l'esistenza dei file richiesti, convalidare package.json ha dipendenze dal framework, garantire che la struttura del progetto segua le convenzioni di apps-in-toss o eseguire il debug di problemi di configurazione del progetto.
