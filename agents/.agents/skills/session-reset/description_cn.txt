重置会话，同时保留上下文。提交更改，编写捕获当前工作状态的 PROMPT.md，拆除任何活跃团队（如果存在），然后引导用户完成 /clear 和上下文恢复。支持“new”参数（“/session-reset new”），仅写入关键的机构知识，而无需当前任务/进度 - 用于在保留学习内容的同时开始不同的任务。当上下文窗口变大、会话需要重新启动或切换焦点时使用。当用户说“重置”、“重新启动”、“保存并清除”或“写入提示”时也可使用。
