Diagnostica e correggi i bug con domande adattative usando `question` (scelta multipla + risposta libera). Usar quando l'utente segnala che qualcosa non funziona, ci sono errori nell'interfaccia utente/API/dati o un comportamento insperato, soprattutto con poco contesto iniziale.
