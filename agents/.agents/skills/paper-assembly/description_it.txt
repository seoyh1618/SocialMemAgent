Orchestra l'intera pipeline della carta end-to-end. Gestire la propagazione dello stato tra le fasi (letteratura → piano → codice → esperimenti → figure → tabelle → scrittura → revisione), supportare il checkpoint e la ripresa. Utilizzare per assemblare un documento completo dai componenti.
