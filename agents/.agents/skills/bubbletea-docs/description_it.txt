Bubble Tea è un framework Go per la creazione di eleganti interfacce utente terminali (TUI).
Da utilizzare durante la creazione di applicazioni CLI con menu interattivi, moduli, elenchi, tabelle o qualsiasi interfaccia utente del terminale.
Basato sull'architettura Elm (Model-View-Update). Caratteristiche principali: input da tastiera/mouse, layout reattivi, 
comandi asincroni, componenti Bubbles (spinner, elenco, tabella, finestra, input di testo, avanzamento).
Include lucidalabbra per lo styling e Huh per le forme.
