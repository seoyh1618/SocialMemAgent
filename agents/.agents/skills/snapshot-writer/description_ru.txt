Напишите одностраничный снимок литературы (`output/SNAPSHOT.md`) из небольшого основного набора + план только с маркерами.
**Триггер**: снимок, снимок литературы, 速览, снимок за 48 часов, снимок на одну страницу, SNAPSHOT.md.
**Используйте, когда**: 你要在 24–48 часов 内交付一个“可读的研究速览” (сначала пуля, 含关键引用）, 而不是完整 опрос).
**Пропустить, если**: 你已经进入 опрос на основе фактических данных 写作（有 `outline/evidence_drafts.jsonl` / `citations/ref.bib` / `output/DRAFT.md`）, 应改用 `subsection-writer`/`prose-writer`。
**Сеть**: нет.
**Ограждение**: 不发明论文/引用；引用只来自 `papers/core_set.csv`（或同 рабочая область的候选池）；不写长段落（避免“像综述生成器”）。
