Bearbeiten Sie GitHub PR-Titel/-Text (und optional Kommentare) zuverlässig unter Windows. Wird verwendet, wenn Sie aufgefordert werden, PR-Metadaten zu aktualisieren, oder wenn die GitHub-API aufgrund fehlender Authentifizierung 404 zurückgibt (private Repos geben häufig 404 zurück, wenn sie nicht authentifiziert sind).
