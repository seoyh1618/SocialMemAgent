通过将“范围内”引用注入现有草案（无新事实）来应用“引用多样化”预算报告，这样运行就可以通过全球唯一引用门，而不会出现引用转储。
**触发器**：引文注入器、应用引文预算、注入引文、安全添加引文、引用注入、按预算加引用、引用增密。
**当**使用：`output/CITATION_BUDGET_REPORT.md`存在，并且您需要在`draft-polisher`/`pipeline-auditor`之前提高*全局*唯一引用（或减少过度重用）。
**如果**则跳过：您需要上游更多论文/引文（首先修复 C1/C2 映射），或者缺少“引用/ref.bib”。
**网络**：无。
**护栏**：没有新事实；不要发明引用；仅注入“引用/ref.bib”中存在的键；将注入的引用保持在每个 H3 允许的范围内（通过预算报告）；避免引用转储段落（在每个作品中嵌入引用）。
