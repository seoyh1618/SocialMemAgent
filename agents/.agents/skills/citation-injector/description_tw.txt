通過將“範圍內”引用注入現有草案（無新事實）來應用“引用多樣化”預算報告，這樣運行就可以通過全球唯一引用門，而不會出現引用轉儲。
**觸發器**：引文注入器、應用引文預算、注入引文、安全添加引文、引用注入、按預算加引用、引用增密。
**當**使用：`output/CITATION_BUDGET_REPORT.md`存在，並且您需要在`draft-polisher`/`pipeline-auditor`之前提高*全局*唯一引用（或減少過度重用）。
**如果**則跳過：您需要上游更多論文/引文（首先修復 C1/C2 映射），或者缺少“引用/ref.bib”。
**網絡**：無。
**護欄**：沒有新事實；不要發明引用；僅注入“引用/ref.bib”中存在的鍵；將注入的引用保持在每個 H3 允許的範圍內（通過預算報告）；避免引用轉儲段落（在每個作品中嵌入引用）。
