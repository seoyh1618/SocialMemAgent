Despliega un flujo ágil como microservicio FastAPI interno en Coolify, accesible solo desde n8n a través de red interna Docker (sin URL pública). Crea automáticamente el repositorio privado en GitHub, sube el código, crea la aplicación en Coolify y verifica que el healthcheck esté activo. Usa esta habilidad siempre que el usuario quiera "desplegar", "subir a producción", "publicar el agente", "hacer que n8n llame al microservicio", "crear el repo", o cualquier variación de convertir código local en un servicio corriendo. También se aplica cuando el usuario dice cosas como "ya terminé el agente, ahora qué" o "cómo lo integro con n8n".
