Fornisce un flusso agente come microservizio FastAPI interno su Coolify, accessibile solo da n8n tramite Docker interno rosso (senza URL pubblico). Crea automaticamente il repository privato su GitHub, inserisci il codice, crea l'applicazione su Coolify e verifica che il controllo sanitario sia attivo. Usa este skill sempre che l'utente voglia "desplegar", "subir a producción", "publicar el agente", "hacer que n8n llame al microservicio", "crear el repo", o qualsiasi variazione di convertire il codice locale in un servizio corriendo. Applicalo anche quando l'utente dice cose come "hai terminato l'agente, ora cosa" o "come lo integro con n8n".
