Извлеките данные исследования в структурированную таблицу (papers/extraction_table.csv), используя схему извлечения протокола.
**Триггер**: форма извлечения, таблица извлечения, извлечение данных, 信息提取, 提取表.
**Использовать, когда**: систематический обзор, скрининг, экстракция (C3), 需要把纳入论文按字段落到 CSV или синтез.。
**Пропустить, если**: 还没有 `papers/screening_log.csv` и протокол 未锁定。
**Сеть**: нет.
**Ограждение**: 严格按 схема 填字段；不要在此阶段写 повествовательный синтез (那是 `синтез-писатель`)。
