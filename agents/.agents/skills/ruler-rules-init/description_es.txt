Migre y arranque reglas de IA basadas en reglas en todos los repositorios con plantillas en inglés, detección de proyectos preestablecida y configuración segura e idempotente. Úselo al crear o estandarizar archivos `.ruler/*`, generar `AGENTS.md`/`CLAUDE.md`, conectar `ruler:apply` a `package.json`, agregar archivos generados ignorados en `.gitignore` y diseñar patrones de alcance de `applyTo` como `**` y `web/**`.
