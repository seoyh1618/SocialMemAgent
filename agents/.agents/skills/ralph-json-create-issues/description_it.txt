Converte un file PRD o un file di markdown del piano in formato prd.json affinché ralph-json-start-loop possa essere eseguito in modo autonomo. Da utilizzare quando l'utente desidera convertire un PRD o un piano in storie JSON.
