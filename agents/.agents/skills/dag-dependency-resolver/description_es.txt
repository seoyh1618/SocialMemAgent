Valida estructuras DAG, realiza clasificación topológica, detecta ciclos y resuelve conflictos de dependencia. Utiliza el algoritmo de Kahn para un orden de ejecución óptimo. Activar en 'resolver dependencias', 'clasificación topológica', 'detección de ciclo', 'orden de dependencia', 'validar dag'. NO para crear DAG (use dag-graph-builder) o programar la ejecución (use dag-task-scheduler).
