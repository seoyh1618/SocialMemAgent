هام: يُستخدم لجميع أسئلة Makepad/Robius بما في ذلك الأدوات والتخطيط والأحداث والتظليل. المشغلات على: makepad، robius، Live_design، app_main، Widget، عرض، زر، تسمية، صورة، TextInput، ScrollView، RoundedView، SolidView، PortalList، Markdown، Html، TextFlow، تخطيط، Flow، Walk، padding، هامش، عرض، ارتفاع، Fit، شغل، محاذاة، تباعد، حدث، إجراء، Hit، FingerDown، FingerUp، KeyDown، Handle_event، انقر، اضغط، الرسوم المتحركة، الرسوم المتحركة، الحالة، الانتقال، التحويم، الضغط، سهولة، التظليل، draw_bg، draw_text، Sdf2d، بكسل، التدرج، التوهج، الظل، الخط، text_style، Font_size، الصورة الرمزية، الطباعة، tokio، async، Spawn، Submit_async، SignalToUI، post_action، Apply_over، TextOrImage، modal، Collapsible، Drag drop، AppState، استمرار، سمة، النطاق، النشر، الحزمة، APK، IPA، WASM، makepad البضائع، أداة makepad، makepad 组件، makepad 按钮، makepad 布局، makepad 事件، makepad 动画، makepad 着色器، 创建组件،自定 义组件, 开发应用, 居中, 对齐, 点击事件, 悬停效果, 渐变, 阴影, 字体大小
