Zig C 互操作技能。从 Zig 调用 C、从 C 调用 Zig、使用 @cImport 和 @cInclude、在 C 标头上运行 translate-c、定义外部结构和打包结构、匹配 C ABI 类型或构建混合 C/Zig 项目时使用。在有关 @cImport、@cInclude、translate-c、extern struct、packed struct、Zig C ABI、从 Zig 调用 C、将 Zig 导出到 C 或 bindgen 等效项的查询时激活。
