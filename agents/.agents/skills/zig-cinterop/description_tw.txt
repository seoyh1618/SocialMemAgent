Zig C 互通技能。從 Zig 呼叫 C、從 C 呼叫 Zig、使用 @cImport 和 @cInclude、在 C 標頭上執行 translate-c、定義外部結構和打包結構、匹配 C ABI 類型或建立混合 C/Zig 專案時使用。在有關 @cImport、@cInclude、translate-c、extern struct、packed struct、Zig C ABI、從 Zig 呼叫 C、將 Zig 匯出到 C 或 bindgen 等效項的查詢時啟動。
