Test API con curl e httpie per richieste HTTP. Da utilizzare quando l'utente chiede di "testare l'API", "effettuare una richiesta HTTP", "arricciare POST", "inviare una richiesta", "testare l'endpoint", "effettuare il debug dell'API" o effettuare chiamate HTTP dalla riga di comando.
