将核心集中的论文映射到大纲的每个小节，并编写“outline/mapping.tsv”来跟踪覆盖范围。  
**触发器**：部分映射器、mapping.tsv、覆盖范围、纸张到部分映射、纸张映射、覆盖范围。  
**何时使用**：结构阶段（C2），当 `papers/core_set.csv` + `outline/outline.yml` 已经存在时，确保每个小节在进入证据/写作之前都有足够的支持论文。  
**如果**则跳过：尚无大纲（首先运行 `outline-builder`）或核心集尚未收敛。  
**网络**：无。  
**护栏**：覆盖范围必须是可审计的（避免在所有小节中重复使用同一批次的论文）；为覆盖较弱的小节留下明确的修复方向（展开查询/合并小节）。
