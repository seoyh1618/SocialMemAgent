Azure AI Search SDK لـ .NET (Azure.Search.Documents). يستخدم لإنشاء تطبيقات البحث باستخدام البحث عن النص الكامل والمتجه والدلالي والمختلط. يغطي SearchClient (الاستعلامات، المستند CRUD)، وSearchIndexClient (إدارة الفهرس)، وSearchIndexerClient (المفهرسات، ومجموعات المهارات). المشغلات: "Azure Search .NET"، و"SearchClient"، و"SearchIndexClient"، و"vector search C#"، و"semantic search .NET"، و"hybrid search"، و"Azure.Search.Documents".
