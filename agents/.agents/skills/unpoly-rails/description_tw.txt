Unpoly 的 Ruby on Rails 整合。在使用 unpoly-rails gem 或建立 Unpoly 支援的 Rails 應用程式時使用。涵蓋伺服器端助理（up?、up.target、up.layer.accept、up.layer.dismiss、up.layer.open、up.validate?、up.cache.expire、up.context、up.emit、up.safe_callback、fresh_when、render_nothing、up.emit、up.safe_callback、fresh_when、render_nothing）、Rails 檢視[up-hungry] 的 flash 訊息、Turbo 共存（停用） Rails 7+ 中的 Turbo Drive）、使用 csp_meta_tag 的 CSP 設定以及全域後續配置。
