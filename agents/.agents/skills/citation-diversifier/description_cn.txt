提高引文多样性/密度（无新事实）：为每个 H3 制定范围内的“引文预算”计划，以便草稿不再未能通过全球唯一引文门，不再显得被引用不足。
**触发**：引用增加、引用预算、独特引用太低、添加更多引用、提高参考密度、引用太少、增加引用、引用密度。
**在以下情况下使用**：“管道审核员”由于独特引用率低而失败，或者您希望在不更改声明的情况下增加引用密度。
**如果**则跳过：您需要新论文（首先修复 C1/C2 映射），或者缺少 `itations/ref.bib` / `outline/writer_context_packs.jsonl` 。
**网络**：无。
**护栏**：没有新事实；不要发明引用；只使用 `itations/ref.bib` 中已经存在的键；将引用保留在每个 H3 允许的范围内（`outline/writer_context_packs.jsonl` / `outline/evidence_bindings.jsonl`）。
