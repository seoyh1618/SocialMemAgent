提高引文多樣性/密度（無新事實）：為每個 H3 制定範圍內的“引文預算”計劃，以便草稿不再未能通過全球唯一引文門，不再顯得被引用不足。
**觸發**：引用增加、引用預算、獨特引用太低、添加更多引用、提高參考密度、引用太少、增加引用、引用密度。
**在以下情況下使用**：“管道審核員”由於獨特引用率低而失敗，或者您希望在不更改聲明的情況下增加引用密度。
**如果**則跳過：您需要新論文（首先修復 C1/C2 映射），或者缺少 `itations/ref.bib` / `outline/writer_context_packs.jsonl` 。
**網絡**：無。
**護欄**：沒有新事實；不要發明引用；只使用 `itations/ref.bib` 中已經存在的鍵；將引用保留在每個 H3 允許的範圍內（`outline/writer_context_packs.jsonl` / `outline/evidence_bindings.jsonl`）。
