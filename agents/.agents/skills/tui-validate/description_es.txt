Valida la salida de la interfaz de usuario del terminal (TUI) mediante la congelación para captura de pantalla y LLM como juez para validación semántica. Admite modos de validación tanto visuales (PNG/SVG) como basados ​​en texto.
