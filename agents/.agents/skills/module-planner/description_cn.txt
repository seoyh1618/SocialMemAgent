从概念图中规划教程模块，包括模块目标和排序，保存为“outline/module_plan.yml”。
**触发器**：模块计划、教程模块、课程大纲、模块规划、module_plan.yml。
**使用时**：教程管道的结构阶段（C2），已有 `outline/concept_graph.yml`，需要把概念依赖转成可教学的模块序列。
**如果**则跳过：还没有概念图（先跑 `concept-graph`）。
**网络**：无。
**Guardrail**：每个模块明确的目标+输出（最好含运行示例步骤）；避免散文段落。
