Nur manueller Operatorbefehl für Cardano CLI: Wird direkt an das OpenClaw Exec Tool (kein Modell) gesendet, sodass Sie deterministische, genehmigungsgesteuerte Cardano-CLI-Befehle ausführen können (nativ oder Docker-Fallback).
