استخدم ADC (APISIX Declarative CLI) لإدارة جميع موارد Apache APISIX. يتم تشغيله عندما يذكر المستخدم "APISIX"، و"إدارة APISIX"، و"تكوين APISIX"، و"مسار APISIX"، و"خدمة APISIX"، و"APISIX upstream"، و"APISIX Consumer"، و"APISIX plugin"، و"APISIX SSL"، و"APISIX gate"، و"القواعد العامة"، و"install adc"، و"adc ping"، و"adc sync"، و"adc diff"، و"adc dump"، و"adc lint"، و"إنشاء مسار"، و"add" المستهلك)، أو "تكوين المنبع"، أو "تحديد المعدل"، أو "مصادقة واجهة برمجة التطبيقات"، أو "بوابة واجهة برمجة التطبيقات"، أو أي مهمة تكوين APISIX/ADC.
