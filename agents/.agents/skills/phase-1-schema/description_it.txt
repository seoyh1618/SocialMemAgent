Capacità di definire la terminologia e le strutture dei dati utilizzate durante il progetto.
Copre la terminologia del dominio, le entità, le relazioni e la progettazione dello schema.

Utilizzare in modo proattivo quando si inizia un nuovo progetto o quando le strutture dei dati non sono chiare.

Trigger: schema, terminologia, modello dati, entità, 스키마, 用語, データモデル, 数据模型,
esquema, terminologia, modello di dati, schema, terminologia, modello di dati,
Schema, terminologia, modello dati, schema, terminologia, modello dati

NON utilizzare per: modifiche solo dell'interfaccia utente, distribuzione o quando lo schema è già definito.
