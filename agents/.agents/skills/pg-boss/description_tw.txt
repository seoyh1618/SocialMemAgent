pg-boss 專家，用於 PostgreSQL 支持的作業隊列，具有一次性交付，非常適合已經使用 Postgres 的應用程序（Supabase、Neon 等）。當提到“pg-boss、postgres 隊列、postgresql 作業、supabase 後台作業、neon 作業隊列、postgres 調度、數據庫作業隊列、pg-boss、postgresql、作業隊列、後台作業、supabase、neon、恰好一次、調度”時使用。
