Questa competenza fornisce una conoscenza completa per la creazione di applicazioni con Cloudflare Sandboxes SDK, che consente l'esecuzione sicura e isolata di codice in contenitori Linux completi all'edge. Dovrebbe essere utilizzato durante l'esecuzione di codice non attendibile, l'esecuzione di script Python/Node.js, l'esecuzione di operazioni git, la creazione di sistemi di esecuzione di codice AI, la creazione di ambienti di sviluppo interattivi o l'implementazione di flussi di lavoro CI/CD che richiedono funzionalità complete del sistema operativo.

Da utilizzare quando: configurazione di sandbox Cloudflare, esecuzione sicura di codice Python/Node.js, gestione di ambienti di sviluppo stateful, implementazione di interpreti di codice AI, esecuzione di comandi shell in isolamento, gestione di repository git in modo programmatico, creazione di agenti di codifica basati su chat, creazione di ambienti di build temporanei, elaborazione di file con strumenti di sistema (ffmpeg, imagemagick, ecc.) o quando si riscontrano problemi con il ciclo di vita del contenitore, la gestione delle sessioni o la persistenza dello stato.

Parole chiave: sandbox cloudflare, esecuzione di contenitori, esecuzione di codice, ambiente isolato, oggetti durevoli, contenitore Linux, esecuzione Python, esecuzione di nodi, operazioni git, interprete di codice, agenti AI, gestione delle sessioni, contenitore temporaneo, area di lavoro, SDK sandbox, @cloudflare/sandbox, exec(), getSandbox(), runCode(), gitCheckout(), contenitore ubuntu
