Erstellen Sie benutzerdefinierte Komponenten, die sich in das Design, die Styles-API und die Kernfunktionen von Mantine integrieren lassen. Verwenden Sie diese Fertigkeit, wenn: (1) eine neue Komponente mit „factory()“, „polymorphicFactory()“ oder „genericFactory()“ erstellt wird, (2) Styles-API-Unterstützung hinzufügt (Klassennamen, Stile, Variablen, nicht gestylt), (3) CSS-Variablen über „createVarsResolver“ implementiert, (4) zusammengesetzte Komponenten mit Unterkomponenten und gemeinsamem Kontext erstellt, (5) eine Komponente bei MantineProvider über Component.extend() registriert wird oder (6) jede Aufgabe mit Factory, useProps, Verwenden Sie Styles, BoxProps, StylesApiProps oder ElementProps in @mantine/core.
