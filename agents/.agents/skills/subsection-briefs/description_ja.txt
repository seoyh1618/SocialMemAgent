サブセクションごとに執筆要旨を作成します (文章は不要)。これにより、後の草案作成は証拠とチェック可能な比較軸 (アウトラインのプレースホルダーではありません) に基づいて行われます。
**トリガー**: サブセクション ブリーフ、ライティング カード、インテント カード、H3 ブリーフ、scope_rule、軸、クラスター、写作意图卡、小节卡片、段落计划。
**次の場合に使用します**: `outline/outline.yml` + `outline/mapping.tsv` + `papers/paper_notes.jsonl` が存在し、テンプレートを漏らすことなくセクションごとのドラフトを作成したい場合。
**次の場合はスキップします**: `outline/subsection_briefs.jsonl` がすでに存在し、洗練されています (プレースホルダー/省略記号なし、axes+clusters+paragraph_plan が埋められています)。
**ネットワーク**: なし。
**ガードレール**: 散文禁止。論文を発明しないでください。 `papers/paper_notes.jsonl` に存在する `paper_id`/`bibkey` のみを参照します。
