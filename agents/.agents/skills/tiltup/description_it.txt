Avvia l'ambiente di sviluppo Tilt in tmux, monitora il bootstrap allo stato integro, correggi i bug di Tiltfile senza hardcoding o fallback. Da utilizzare quando si avvia tilt, si esegue il debug degli errori Tiltfile o si avvia un ambiente di sviluppo.
