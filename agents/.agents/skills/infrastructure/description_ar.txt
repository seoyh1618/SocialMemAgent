إدارة البنية التحتية لنظام NixOS لمشروع رقائق nix هذا. نشر التكوينات مع Colmena، وإدارة حاويات Proxmox LXC، واستكشاف أخطاء الخدمات وإصلاحها، وصيانة الخوادم.

يُستخدم عندما: (1) نشر تكوينات NixOS مع colmena، (2) إدارة حاويات Proxmox LXC (البدء والإيقاف وإعادة التشغيل والحالة)، (3) استكشاف مشكلات الخادم وإصلاحها عبر SSH أو pct exec، (4) التحقق من حالة الخدمة عبر الأجهزة المضيفة، (5) أي مهمة لصيانة البنية التحتية.

ملاحظات معمارية هامة:
- جميع الخوادم عبارة عن حاويات Proxmox LXC.
