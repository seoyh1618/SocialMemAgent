قم ببناء خوادم MCP (بروتوكول السياق النموذجي) باستخدام Python SDK الرسمي. يغطي واجهة برمجة تطبيقات FastMCP عالية المستوى مع @mcp.tool() و@mcp.resource() و@mcp.prompt() وتكامل FastAPI/Starlette ووسائل النقل (stdio وSSE وstreamable-http) وتكامل قاعدة البيانات.
