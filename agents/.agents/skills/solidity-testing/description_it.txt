[INVOCA AUTOMATICA] DEVE essere invocato PRIMA di scrivere o modificare qualsiasi file di test (*.t.sol). Copre la struttura dei test, le convenzioni di denominazione, i requisiti di copertura, i test fuzz e i cheatcode Foundry. Trigger: qualsiasi attività che coinvolga la creazione, la modifica o l'esecuzione di test di solidità.
