À utiliser lorsque chaque SDK de langue réside dans un référentiel distinct. Couvre l'envoi de flux de travail entre dépôts, les rapports sur l'état des relations publiques, le rapprochement des relations publiques lors de la fusion/fermeture. Déclencheurs sur le « SDK multi-repo », les « référentiels SDK séparés », les « workflows cross-repo », la « synchronisation SDK PR », « le dépôt de spécifications déclenche les dépôts SDK ».
