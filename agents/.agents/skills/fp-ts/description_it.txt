Padroneggia la libreria fp-ts per la programmazione funzionale digitata in TypeScript, inclusi Option, Both, Task, TaskEither, Reader, State, IO, Array, Record, composizione di tubi/flussi, notazione Do, ottica (lenti/prismi) e integrazione con l'ecosistema Effect-TS. Da utilizzare quando si lavora con tipi di dati fp-ts, si compongono pipeline funzionali, si gestiscono gli effetti in modo funzionale, si implementano modelli monadici o si utilizzano utilità fp-ts per codice funzionale indipendente dai tipi.
