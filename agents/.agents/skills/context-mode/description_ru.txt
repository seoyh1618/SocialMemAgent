Используйте инструменты контекстного режима (ctx_execute, ctx_execute_file) вместо Bash/cat при обработке.
большие объемы производства. Триггерные фразы: «проанализировать журналы», «обобщить выходные данные», «обработать данные»,
«анализировать JSON», «фильтровать результаты», «извлекать ошибки», «проверять выходные данные сборки»,
«анализ зависимостей», «обработка ответа API», «анализ больших файлов»,
«извлечь элементы», «снимок страницы», «снимок браузера», «сделать снимок»,
«Структура DOM», «страница проверки», «поля формы», «селекторы элементов»,
«структура веб-страницы», «дерево доступности», «снимок драматурга»,
«запустить тесты», «выходные данные теста», «отчет о покрытии», «журнал git», «недавние коммиты»,
«разница между ветвями», «список контейнеров», «статус модуля», «использование диска»,
«извлечь документы», «ссылка на API», «индексная документация», «обращение к конечной точке»,
«вызов API», «проверка ответа», «результаты запроса», «показать таблицы»,
«найти TODO», «подсчитать строки», «статистика кодовой базы», «аудит безопасности»,
«устаревшие пакеты», «дерево зависимостей», «облачные ресурсы», «выход CI/CD».
Также срабатывает при выводе ЛЮБОГО инструмента MCP (Playwright, Context7, GitHub API), который
может превышать 20 строк, а также любая операция, размер вывода которой неизвестен.
Маршрутизация субагентов обрабатывается автоматически с помощью ловушки PreToolUse — имена инструментов вручную в подсказках не требуются.
