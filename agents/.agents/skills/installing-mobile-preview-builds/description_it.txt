Crea e installa la build di anteprima iOS per app/dispositivi mobili utilizzando la build locale EAS e devicectl. Da utilizzare quando l'utente chiede di installare un'anteprima/build iOS interna su un iPhone connesso per test di tipo produttivo.
