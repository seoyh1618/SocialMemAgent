為調查定義證據優先的表模式：每個表必須回答什麼、行單位、列以及填充它需要哪些證據包字段。
**觸發器**：表模式、模式優先表、表設計、表格模式、先模式後填充。
**使用時間**：您希望調查表在 LaTeX 之前可驗證且可填寫（通常是階段 C4，在證據包存在之後）。
**如果**則跳過： `outline/table_schema.md` 已存在並已完善（涵蓋索引表和附錄表；無佔位符；證據映射是顯式的）。
**網絡**：無。
**護欄**：沒有捏造事實；架構必須是可檢查的並將每列映射到證據源。
