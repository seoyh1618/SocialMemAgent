为调查定义证据优先的表模式：每个表必须回答什么、行单位、列以及填充它需要哪些证据包字段。
**触发器**：表模式、模式优先表、表设计、表格模式、先模式后填充。
**使用时间**：您希望调查表在 LaTeX 之前可验证且可填写（通常是阶段 C4，在证据包存在之后）。
**如果**则跳过： `outline/table_schema.md` 已存在并已完善（涵盖索引表和附录表；无占位符；证据映射是显式的）。
**网络**：无。
**护栏**：没有捏造事实；架构必须是可检查的并将每列映射到证据源。
