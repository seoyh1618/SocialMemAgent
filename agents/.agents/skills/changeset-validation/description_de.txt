Validieren Sie Änderungssätze in openai-agents-js mithilfe der LLM-Beurteilung anhand von Git-Diffs (einschließlich nicht festgeschriebener lokaler Änderungen). Wird verwendet, wenn Pakete/ oder .changeset/ geändert werden oder wenn PR-Änderungssatzkonformität und Bump-Level überprüft werden.
