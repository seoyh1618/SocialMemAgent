تحسين عمليات تحسين موارد GPU. مهارة التنشيط التلقائي لنشر ML.
يتم تشغيله: مُحسِّن موارد وحدة معالجة الرسومات (GPU)، ومُحسِّن موارد وحدة معالجة الرسومات (GPU).
جزء من فئة مهارات نشر ML. يُستخدم عند العمل مع وظيفة مُحسِّن موارد وحدة معالجة الرسومات (GPU). قم بتشغيل عبارات مثل "مُحسِّن موارد gpu" و"مُحسِّن gpu" و"gpu".
