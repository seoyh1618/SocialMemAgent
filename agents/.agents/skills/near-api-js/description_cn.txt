使用near-api-js v7 进行开发的指南 - 用于 NEAR 区块链交互的 JavaScript/TypeScript 库。  在以下情况下使用：(1) 构建与 NEAR 区块链交互的应用程序，(2) 创建/签署交易，(3) 调用智能合约，(4) 管理账户和密钥，(5) 使用 NEAR RPC API，(6) 在 NEAR 上处理 FT/NFT 代币，(7) 使用 NEAR 加密操作（KeyPair、签名），(8) 在 NEAR 单位之间进行转换（yocto、gas），(9) 使用中继器进行无gas/元交易，(10) 钱包集成和会话管理，(11) 用于身份验证的 NEP-413 消息签名，(12) FT 合约的存储押金管理。触发任何 NEAR 区块链开发任务。
