Híbrido RNN+Transformer con inferencia O(n). Tiempo lineal, contexto infinito, sin caché KV. Entrene como GPT (paralelo), infiera como RNN (secuencial). Proyecto de IA de la Fundación Linux. Producción en Windows, Office, NeMo. RWKV-7 (marzo de 2025). Modelos de hasta 14B de parámetros.
