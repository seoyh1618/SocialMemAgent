Initialisieren und konfigurieren Sie LangGraph-Projekte mit der richtigen Struktur, langgraph.json-Konfiguration, Umgebungsvariablen und Abhängigkeitsmanagement. Verwenden Sie diese Option, wenn Benutzer (1) ein neues LangGraph-Projekt erstellen, (2) langgraph.json für die Bereitstellung einrichten, (3) Umgebungsvariablen für LLM-Anbieter konfigurieren, (4) die Projektstruktur für Agenten initialisieren, (5) die lokale Entwicklung mit LangGraph Studio einrichten, (6) Abhängigkeiten konfigurieren möchten (pyproject.toml, require.txt, package.json) oder (7) Projektkonfigurationsprobleme beheben möchten.
