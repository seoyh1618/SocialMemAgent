Minimiza las reproducciones innecesarias de React cuando consumes estados externos (XState, @xstate/store, Zustand, Redux, Nanostores, context). Prefiera las suscripciones basadas en selectores a useState(wholeObject). Úselo cuando trabaje con estados externos en React, optimice re-renderizaciones, elija patrones de estado o integre con estas bibliotecas.
