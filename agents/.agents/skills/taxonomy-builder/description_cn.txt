根据核心论文集和范围约束构建 2+ 级分类法 (`outline/taxonomy.yml`)，每个节点都有简短描述。  
**触发器**：分类、分类构建器、分类、主题树、taxonomy.yml。  
**使用时间**：调查/快照的结构阶段（无散文），“papers/core_set.csv”已存在，需要可映射且读者友好的主题结构。  
**如果**跳过：已批准且可映射的分类法已经存在（没有无意义的重构）。  
**网络**：无。  
**护栏**：避免通用占位符桶；为每个节点保留 2 个以上级别和具体描述。
