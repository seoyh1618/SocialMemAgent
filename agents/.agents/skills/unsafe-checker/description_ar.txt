حاسم: يُستخدم لمراجعة كود Rust غير الآمن وFFI. المشغلات على: غير آمن، مؤشر خام، FFI، خارجي، تحويل، *mut، *const، union، #[repr(C)]، libc، std::ffi، MaybeUninit، NonNull، تعليق SAFETY، سلامة، سلوك غير محدد، UB، غلاف آمن، تخطيط الذاكرة، bindgen، cbindgen، CString، CStr، 安全抽象،裸指针, 外部函数接口, 内存布局, 不安全代码, FFI 绑定, 未定义行为
