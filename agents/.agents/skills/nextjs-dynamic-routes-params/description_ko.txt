Next.js 앱 라우터 동적 경로 및 경로 이름 매개변수에 대한 가이드입니다. URL 세그먼트(ID, 슬러그, 중첩 경로)에 의존하는 페이지를 구축하거나 `params` prop에 액세스하거나 식별자로 리소스를 가져올 때 사용합니다. 가장 간단한 경로 구조(예: URL에서 요구하지 않는 한 `app/products/[id]` 대신 `app/[id]`)를 기본값으로 설정하여 과도한 중첩을 방지하는 데 도움이 됩니다.
