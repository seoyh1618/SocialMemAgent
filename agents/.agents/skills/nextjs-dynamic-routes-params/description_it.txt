Guida per le rotte dinamiche e i parametri del nome del percorso del router dell'app Next.js. Da utilizzare quando si creano pagine che dipendono da segmenti URL (ID, slug, percorsi nidificati), si accede alla prop `params` o si recuperano risorse tramite identificatore. Aiuta a evitare l'annidamento eccessivo impostando per impostazione predefinita la struttura di instradamento più semplice (ad esempio, `app/[id]` invece di `app/products/[id]` a meno che l'URL non lo richieda).
