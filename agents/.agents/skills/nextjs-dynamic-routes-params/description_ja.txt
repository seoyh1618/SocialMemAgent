Next.js App Router の動的ルートとパス名パラメーターのガイド。 URL セグメント (ID、スラッグ、ネストされたパス) に依存するページを構築する場合、「params」プロパティにアクセスする場合、または識別子によってリソースを取得する場合に使用します。デフォルトで最も単純なルート構造 (例: URL で要求されない限り、`app/products/[id]` ではなく `app/[id]`) を使用することで、過剰なネストを回避できます。
