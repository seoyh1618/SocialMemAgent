Erstellt Geschichten aus dem IDEAL-Plan (CREATE) oder fügt vom Benutzer angeforderte Geschichten hinzu (ADD). Erstellt 8-teilige Dokumente, validiert INVEST und erstellt in Linear. Wird von ln-220 aufgerufen.
