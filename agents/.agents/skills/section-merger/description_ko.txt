`sections/` 아래의 섹션별 파일을 `output/DRAFT.md`로 결정적으로 병합하여 개요 순서를 유지하고 `outline/transitions.md`에서 전환을 엮습니다.
**트리거**: 섹션 병합, 초안 병합, 섹션 파일 결합, 섹션/ -> 출력/DRAFT.md, 합并小节, 拼接草稿.
**언제 사용**: `sections/` 아래에 단위별 산문 파일이 있고 다듬기/검토/LaTeX를 위한 단일 `output/DRAFT.md`가 필요합니다.
**다음 경우 건너뛰기**: 섹션 파일이 누락되었거나 여전히 스캐폴딩 마커를 포함하고 있습니다(먼저 `subsection-writer` 수정).
**네트워크**: 없음.
**가드레일**: 결정론적 병합만 가능(새로운 사실/인용 없음) `outline/outline.yml`에서 섹션 순서를 유지합니다.
