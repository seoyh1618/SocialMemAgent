Da utilizzare quando gli stessi dati vengono recuperati ripetutamente. Da utilizzare quando le query del database sono lente. Da utilizzare quando si implementa la memorizzazione nella cache senza strategia di invalidamento.
