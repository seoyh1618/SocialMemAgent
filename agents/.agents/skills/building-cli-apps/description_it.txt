Utilizzare durante la creazione di strumenti dell'interfaccia della riga di comando; quando si sceglie l'argomento per l'analisi delle librerie; quando si gestiscono modelli stdin/stdout/stderr; quando si implementano i sottocomandi; quando i test per le app CLI falliscono o mancano
