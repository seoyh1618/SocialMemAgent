Meilleures pratiques expertes en GDScript, notamment le typage statique (var x : int, func renvoie void), l'architecture du signal (appel du signal vers le bas), l'accès unique au nœud (%NodeName, @onready), la structure du script (extends, class_name, signaux, exportations, méthodes) et les modèles de performances (dict.get avec les valeurs par défaut, évitez get_node dans les boucles). À utiliser pour la révision du code, la refactorisation ou l'établissement de normes de projet. Mots-clés déclencheurs : static_typing, signal_architecture, unique_nodes, @onready, class_name, signal_up_call_down, gdscript_style_guide.
