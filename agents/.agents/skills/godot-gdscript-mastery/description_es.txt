Mejores prácticas de expertos de GDScript que incluyen escritura estática (var x: int, func devuelve void), arquitectura de señal (llamada de inicio de sesión), acceso único al nodo (%NodeName, @onready), estructura de script (extiende, nombre_clase, señales, exportaciones, métodos) y patrones de rendimiento (dict.get con valores predeterminados, evite get_node en bucles). Úselo para revisar código, refactorizar o establecer estándares de proyecto. Palabras clave de activación: static_typing, signal_architecture, Unique_nodes, @onready, class_name, signal_up_call_down, gdscript_style_guide.
