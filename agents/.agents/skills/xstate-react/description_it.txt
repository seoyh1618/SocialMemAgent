Questa abilità dovrebbe essere utilizzata quando l'utente chiede di "creare una macchina a stati", "aggiungere xState", "usare xState con React", "implementare uno stato basato su attori", "gestire stati complessi con macchine a stati", "usare xState con Effect", "integrare Effect-ts con xState", menziona hook xState (useMachine, useActor, useSelector) o discute di macchine a stati finiti nelle applicazioni React.
