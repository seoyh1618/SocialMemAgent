Crea applicazioni desktop GNOME native utilizzando JavaScript (GJS) con GTK 4, Libadwaita e la piattaforma GNOME. Da utilizzare quando l'utente desidera creare, modificare o eseguire il debug di un'app GNOME scritta in JavaScript/GJS, inclusa la progettazione dell'interfaccia utente con XML o Blueprint, sottoclassi GObject, configurazione della build Meson, packaging Flatpak o qualsiasi attività che coinvolga collegamenti GJS per librerie GLib/GIO/GTK4/Adw. Da utilizzare anche quando si lavora con file `.ui`, `meson.build`, GResource XML, schemi GSettings, file `.desktop` o manifest Flatpak in un contesto di progetto GJS.
