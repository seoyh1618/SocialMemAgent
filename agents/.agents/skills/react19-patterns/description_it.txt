Pattern React 19 e comportamento del compilatore React con sintassi abbreviata Context e hook use(). Da utilizzare quando si lavora con Context, useContext, use() hook, componenti Provider, pattern di ottimizzazione come useMemo, useCallback, memo, memoization o quando l'utente menziona React 19, React Compiler, Context.Provider o l'ottimizzazione manuale.
