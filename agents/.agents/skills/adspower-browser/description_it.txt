Esegue le operazioni API locali di AdsPower tramite la CLI del browser adspower (non è richiesto MCP). Utilizzare quando l'utente richiede di creare o gestire browser, gruppi, proxy AdsPower o controllare lo stato; eseguire comandi CLI del nodo che chiamano gli stessi gestori del server MCP.
