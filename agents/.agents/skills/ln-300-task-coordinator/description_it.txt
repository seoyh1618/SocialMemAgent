Orchestra le operazioni delle attività. Analizza la storia, crea un piano ottimale (1-6 attività di implementazione), delega a ln-301-task-creator (CREATE/ADD) o ln-302-task-replanner (REPLAN). Rileva automaticamente l'ID del team.
