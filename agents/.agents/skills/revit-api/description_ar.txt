Revit 2026 API تم تطويره بواسطة Revit API. API، أو API 签名和参数时触发. واجهة برمجة التطبيقات API (3) واجهة برمجة التطبيقات (4) واجهة برمجة التطبيقات (5) واجهة برمجة تطبيقات Revit: RevitAPI، Autodesk.Revit، العنصر، المستند، المعاملة، FilteredElementCollector، ExternalCommand، ExternalApplication، الجدار، الأرضية، FamilyInstance، FamilySymbol، المعلمة، المدمج في المعلمة، المدمج في الفئة، التحديد، GeometryElement، Solid، Face، CurveLoop، Level، View، ViewSheet، XYZ، Line، Arc، IExternalCommand، IExternalApplication، UIApplication، UIDocument، Ribbon، PushButton، ExtensibleStorage، SubTransaction، TransactionGroup، ElementId، Reference، TaskDialog.
