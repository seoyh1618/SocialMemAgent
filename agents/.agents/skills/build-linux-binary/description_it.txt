Crea binari Linux (x64 e ARM64) per XerahS utilizzando lo script di packaging. Gestisce i problemi comuni di blocco dei file, processi obsoleti e precompilazione XAML di Avalonia specifici delle build Linux. Garantisce che i pacchetti deb/rpm vengano creati in dist/.
