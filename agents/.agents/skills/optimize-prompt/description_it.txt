Ottimizza un prompt attraverso una pipeline di compressione critica con verifica dell'equivalenza semantica in ogni fase. Applica il pensiero critico per migliorare il prompt, quindi comprime-prompt per ridurlo, verificando che il comportamento venga preservato dopo ogni trasformazione.
