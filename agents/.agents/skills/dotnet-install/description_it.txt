.NET SDK e installazione runtime su Windows, macOS e Linux. Gestisce il rilevamento della versione, i programmi di installazione specifici della piattaforma (WinGet, Homebrew, apt, dnf), la selezione di SDK e runtime, l'installazione offline, la configurazione di Docker e la risoluzione dei problemi. Si attiva automaticamente per l'installazione, la configurazione, la gestione delle versioni e la distribuzione multipiattaforma di .NET.
