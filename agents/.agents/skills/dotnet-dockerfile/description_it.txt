Crea file Dockerfile in più fasi ottimizzati e sicuri per applicazioni .NET Core e ASP.NET Core. Utilizzare quando (1) si crea un nuovo Dockerfile per un progetto .NET, (2) si inserisce in un contenitore un'applicazione .NET, (3) si ottimizza un Dockerfile .NET esistente, (4) si configura Docker per app .NET 6/7/8+ o (5) l'utente menziona .NET e Docker/contenitore insieme.
