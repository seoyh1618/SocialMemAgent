تكوين backtesting.py للتحقق من صحة SQL Oracle واختبار نمط شريط النطاق. يُستخدم عند تشغيل backtesting.py مقابل نتائج ClickHouse SQL، أو تكوين مُنشئ Backtest()، أو التعامل مع الصفقات المتداخلة، أو الوضع متعدد المواضع، أو التعامل مع NaN الكمي المتداول، أو فرز التجارة، أو التحقق من صحة بوابة Oracle. TRIGGERS - backtesting.py، Backtest()، التحوط، Exclusive_orders، الصفقات المتعددة، المتداخلة، التحقق من صحة Oracle، SQL vs Python، مقارنة التجارة، عدم تطابق سعر الدخول، عدم تطابق عدد الإشارات، الكمية المتداولة NaN، فرز ExitTime، stats._trades، gen600_strategy، Champion_strategy، gen300_strategy، إعداد الحاجز.
