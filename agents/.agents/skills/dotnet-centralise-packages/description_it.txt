Converte una soluzione .NET per utilizzare la gestione centralizzata dei pacchetti (CPM). Da utilizzare quando l'utente vuole centralizzare, consolidare o unificare le versioni del pacchetto NuGet tra progetti.
