إدارة شاملة للبنية التحتية لـ AWS باستخدام Terraform. يغطي تكوين الموفر، وإدارة الحالة (الواجهة الخلفية S3 مع القفل الأصلي في Terraform 1.11+)، وأنماط الموارد المشتركة (VPC، وIAM، وS3، وRDS، وEKS)، واستخدام الوحدة، وأفضل ممارسات الإنتاج. يُستخدم عند توفير البنية التحتية لـ AWS، أو إدارة حالة Terraform، أو إنشاء VPCs، أو تكوين أدوار/سياسات IAM، أو نشر قواعد البيانات، أو استكشاف مشكلات Terraform/AWS وإصلاحها.
