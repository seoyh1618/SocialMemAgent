将分类法​​ (`outline/taxonomy.yml`) 转换为包含部分/小节的仅项目符号大纲 (`outline/outline.yml`)。
**触发器**：大纲生成器、项目符号大纲、outline.yml、大纲生成、仅限项目符号。
**使用时**：结构阶段（NO PROSE），已有分类，需要生成可映射/可写作的章节与小节重构（每小节≥3个项目符号）。
**如果**则跳过：已经批准有过且可映射的轮廓（避免无意义的流失）。
**网络**：无。
**Guardrail**: 仅项目符号；删除 TODO/模板语句；每小节至少 3 个可检查项目符号。
