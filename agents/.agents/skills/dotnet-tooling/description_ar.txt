يدير تثبيت .NET SDK (تثبيت dotnet، أعباء العمل)، إعداد المشروع (.slnx، Directory.Build.props، CPM)، تأليف MSBuild، تحسين البناء، الأداء (Span، ArrayPool، stackalloc)، التوصيف (عدادات dotnet، تتبع dotnet)، AOT/التشذيب الأصلي، ضبط GC، تطبيقات CLI (System.CommandLine، Spectre.Console، Terminal.Gui)، وإلغاء ترجمة ILSpy، وتكوين تصحيح أخطاء VS Code (launch.json، وcoreclr، وremote)، وC# LSP (csharp-ls، وOmniSharp)، واكتشاف/ترقية الإصدار. يمتد إلى 34 مجالًا للموضوع. لا تستخدم لتنفيذ واجهة المستخدم أو تصميم أمان واجهة برمجة التطبيقات.
