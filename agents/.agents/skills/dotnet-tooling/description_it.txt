Gestisce l'installazione di .NET SDK (dotnet-install, carichi di lavoro), l'impostazione del progetto (.slnx, Directory.Build.props, CPM), la creazione di MSBuild, l'ottimizzazione della build, le prestazioni (Span, ArrayPool, stackalloc), la profilazione (dotnet-counters, dotnet-trace), AOT/trimming nativi, ottimizzazione GC, app CLI (System.CommandLine, Spectre.Console, Terminal.Gui), decompilazione ILSpy, Configurazione di debug di VS Code (launch.json, coreclr, remote), C# LSP (csharp-ls, OmniSharp) e rilevamento/aggiornamento della versione. Copre 34 aree tematiche. Non utilizzare per l'implementazione dell'interfaccia utente o la progettazione della sicurezza API.
