Da utilizzare quando l'utente richiede narrazione da testo a voce fuori campo, letture di accessibilità, istruzioni audio o generazione di discorsi in batch tramite l'API OpenAI Audio; esegui la CLI in bundle (`scripts/text_to_speech.py`) con voci integrate e richiedi `OPENAI_API_KEY` per le chiamate dal vivo. La creazione di voci personalizzate non rientra nell'ambito.
