Erstellen Sie optimierte, sichere mehrstufige Docker-Dateien für React-Anwendungen (Vite, CRA, Next.js static). Wird verwendet, wenn (1) eine neue Docker-Datei für ein React-Projekt erstellt wird, (2) eine React/Vite-Anwendung in einen Container umgewandelt wird, (3) eine vorhandene React-Docker-Datei optimiert wird, (4) Docker für React mit Nginx eingerichtet wird oder (5) der Benutzer React und Docker/Container zusammen erwähnt.
