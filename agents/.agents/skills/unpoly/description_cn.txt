使用 Unpoly 构建服务器渲染应用程序的综合指南 — Unpoly 是一个渐进式增强框架，通过 HTML 属性添加类似 SPA 的行为。在处理 Unpoly 片段更新、覆盖/图层、表单、编译器、缓存、延迟加载、动画、过渡、错误处理、轮询、渲染生命周期或服务器集成时使用。涵盖 [up-follow]、[up-target]、[up-layer]、[up-submit]、[up-validate]、up.compiler()、up.render()、up.layer.open()、缓存、预览、乐观渲染等。
