Crea endpoint HTTP per le app CLI Wix. Utilizzare solo quando l'utente richiede specificamente un endpoint back-end. Da utilizzare durante la creazione di endpoint API REST, gestori HTTP backend o logica lato server. I trigger includono API backend, endpoint HTTP, metodi HTTP, gestione dei moduli, caricamenti di file.
