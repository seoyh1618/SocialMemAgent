Предотвращает разрушительные операции Git, требуя резервного копирования и подтверждения. Блокирует git reset --hard, git push --force on main, git clean -fd. Требуется резервное копирование перед git checkout, git reset, git rebase. Активируйте любую деструктивную команду git.
