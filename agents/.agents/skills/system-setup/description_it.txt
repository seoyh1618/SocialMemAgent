Convalida e configura tutte le dipendenze richieste per il Plugin Freedom System. Questa è una competenza AUTONOMA che viene eseguita PRIMA dell'inizio dei flussi di lavoro del plug-in. Verifica la presenza di Python, strumenti di compilazione, CMake, JUCE e pluginval, installando facoltativamente le dipendenze mancanti con l'approvazione dell'utente. La configurazione viene salvata in .claude/system-config.json per essere utilizzata da altre competenze. Da utilizzare quando l'utente menziona configurazione, installazione, dipendenze, strumenti mancanti o quando l'hook SessionStart rileva problemi di configurazione.
