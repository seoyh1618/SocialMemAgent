Da utilizzare quando le attività implicano la creazione, la modifica, l'analisi o la formattazione di fogli di calcolo (`.xlsx`, `.csv`, `.tsv`) utilizzando Python (`openpyxl`, `pandas`), soprattutto quando è necessario conservare e verificare formule, riferimenti e formattazione.
