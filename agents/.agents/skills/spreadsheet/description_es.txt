Úselo cuando las tareas impliquen crear, editar, analizar o formatear hojas de cálculo (`.xlsx`, `.csv`, `.tsv`) usando Python (`openpyxl`, `pandas`), especialmente cuando es necesario conservar y verificar fórmulas, referencias y formatos.
