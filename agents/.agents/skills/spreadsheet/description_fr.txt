À utiliser lorsque les tâches impliquent la création, la modification, l'analyse ou le formatage de feuilles de calcul (`.xlsx`, `.csv`, `.tsv`) à l'aide de Python (`openpyxl`, `pandas`), en particulier lorsque les formules, les références et le formatage doivent être préservés et vérifiés.
