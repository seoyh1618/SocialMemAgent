在需求含糊时自动激活。宁可多问几次也不要错误实现。识别特征（满足其一即可）：需求少于 25 词、无具体文件名/路径、模糊动词（优化、改改、修一下、改进、调整、重构、整理）、不确定表述（有点、也许、随便、快点、简单）、缺少成功标准（没有「以便」「从而」「为了」）、相对概念无上下文（更快、更好、更简单）。输出为结构化 JSON 供 prompt-architect 使用。若用户说「直接做」「别问了」「你自己定」则跳过澄清。
